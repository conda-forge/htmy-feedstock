schema_version: 1

context:
  version: "0.13.0"
  python_min: "3.11"

package:
  name: htmy
  version: ${{ version }}

source:
  - url: https://pypi.org/packages/source/h/htmy/htmy-${{ version }}.tar.gz
    sha256: 7210571b64771b78ee42e2fcf292af05b2a4c011c10c4731ac2d4f0a5977eabb

build:
  number: 0
  noarch: python
  script:
    - ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation --disable-pip-version-check

requirements:
  host:
    - python ${{ python_min }}.*
    - pdm-backend
    - pip
  run:
    - python >=${{ python_min }}
    - anyio >=4.7.0,<5
    - markdown >=3.8,<4

tests:
  - python:
      imports:
        - htmy
      pip_check: true
      python_version: ${{ python_min }}.*

about:
  license: MIT
  license_file: LICENSE
  homepage: https://github.com/volfpeter/htmy
  summary: Async, pure-Python server-side rendering engine.

extra:
  recipe-maintainers:
    - davidbrochart
